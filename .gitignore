# runtime data, never commit
data/
*.db
*.db-shm
*.db-wal
chrome-profile/
posts.json
seen_ids.json
run.log

# local overrides
config.local.yaml

# build
bin/
# root-anchored: a bare "feed-engine" would also match cmd/feed-engine/ and
# silently keep the entrypoint out of the repo.
/feed-engine
/feed-engine.exe
.DS_Store
